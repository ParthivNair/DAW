# Golden render fixtures and audio test assets go through Git LFS
# (run `git lfs install` once locally — see dev/TODO.md Part A1)
tests/fixtures/**/*.wav filter=lfs diff=lfs merge=lfs -text
tests/fixtures/**/*.flac filter=lfs diff=lfs merge=lfs -text
tests/fixtures/**/*.mp3 filter=lfs diff=lfs merge=lfs -text
tests/fixtures/**/*.ogg filter=lfs diff=lfs merge=lfs -text
